{
  "name": "media-playback-rate-control",
  "version": "1.0.0",
  "description": "This browser extension allows users to adjust the playback rate of HTML5 video and audio elements.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
